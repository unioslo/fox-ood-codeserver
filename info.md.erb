Code Server launches inside of an [Apptainer](https://apptainer.org/) container. **Note:** type `ssh localhost` to break out of the Apptainer shell when using the integrated terminal in Code Server.

